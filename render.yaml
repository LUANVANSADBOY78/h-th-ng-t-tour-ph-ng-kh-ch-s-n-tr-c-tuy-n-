services:
  - type: web
    name: travel-booking-platform
    env: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_BASE_PATH
        value: /
