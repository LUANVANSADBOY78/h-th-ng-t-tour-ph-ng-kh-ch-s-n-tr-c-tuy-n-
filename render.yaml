services:
  - type: web
    name: travel-booking-platform
    env: static
    buildCommand: npm install && npx vite build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_BASE_PATH
        value: /
